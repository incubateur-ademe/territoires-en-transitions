# API
## Serve locally
```shell
poetry run serve
```
## Variables d'environnement
- `PRODUCTION` must be `yes` to use postgres, otherwise default to sqlite.
- `POSTGRES_USER`
- `POSTGRES_PASSWORD`
- `POSTGRES_DB`
- `POSTGRES_PORT`
- `POSTGRES_HOST`