import {actionsReferentiels} from './referentiels';
import {epcisActions} from './epcis';
import {ficheActions} from './fiches';

export const actions = {
  referentiels: actionsReferentiels,
  epcis: epcisActions,
  fiches: ficheActions,
};
