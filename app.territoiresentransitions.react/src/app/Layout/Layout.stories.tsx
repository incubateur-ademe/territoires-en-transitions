import Layout from './Layout';

export default {
  component: Layout,
};

export const Exemple = () => (
  <Layout>
    <div style={{display: 'flex', padding: '4rem'}}>
      <p style={{margin: 'auto'}}>Page</p>
    </div>
  </Layout>
);
