import { describe, it } from 'vitest';

describe('AnalysisRunRepository contract', () => {
  it.todo(
    "getLastCompletedRunStart renvoie null tant qu'aucun passage n'est enregistré"
  );
  it.todo(
    'getLastCompletedRunStart renvoie la date de début du dernier passage terminé'
  );
  it.todo(
    "createCompletedRun enregistre la date de début donnée et la date de fin à l'heure de la base"
  );
});
