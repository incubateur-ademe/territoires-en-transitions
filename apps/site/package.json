{
  "name": "@tet/site",
  "version": "4.101.0",
  "nx": {
    "name": "site",
    "targets": {
      "dev": {
        "dependsOn": [
          "^build"
        ],
        "options": {
          "port": 3001
        }
      },
      "build": {
        "options": {
          "command": "next build --webpack"
        }
      }
    },
    "// targets": "to see all targets run: nx show project site --web"
  }
}
