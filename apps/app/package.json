{
  "name": "@/app",
  "nx": {
    "name": "app",
    "targets": {
      "dev": {
        "executor": "@nx/next:server",
        "options": {
          "buildTarget": "app:build",
          "dev": true,
          "port": 3000,
          "webpack": true
        }
      },
      "build": {
        "executor": "@nx/next:build",
        "options": {
          "outputPath": "apps/app/.next",
          "webpack": true
        }
      }
    }
  }
}
