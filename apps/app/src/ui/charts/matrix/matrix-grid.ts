type MatrixGrid = {
  left: number;
  right: number;
  top: number;
  bottom: number;
};

const MATRIX_GRID: MatrixGrid = { left: 96, right: 40, top: 40, bottom: 64 };

export { MATRIX_GRID };
export type { MatrixGrid };
