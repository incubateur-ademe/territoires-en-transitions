type AxisProps = {
  name: string;
  minLabel: string;
  maxLabel: string;
};

const XAxis = (_props: AxisProps): null => null;

const YAxis = (_props: AxisProps): null => null;

export { XAxis, YAxis };
export type { AxisProps };
