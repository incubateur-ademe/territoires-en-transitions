import { appLabels } from '@/app/labels/catalog';
import { InfoTooltip } from '@tet/ui';

export const DemarchePilotesInfoTooltip = () => (
  <InfoTooltip label={appLabels.demarchePilotesTooltip} />
);
