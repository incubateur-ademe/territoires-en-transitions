{
  "name": "@tet/panier",
  "version": "4.101.0",
  "nx": {
    "name": "panier",
    "tags": ["frontend"],
    "targets": {
      "dev": {
        "dependsOn": [
          "^build"
        ],
        "options": {
          "port": 3002
        }
      },
      "build": {
        "options": {
          "command": "next build --webpack"
        }
      }
    },
    "// targets": "to see all targets run: nx show project panier --web"
  }
}
