name: Build panier app

on:
  workflow_call:
    inputs:
      environment:
        required: false
        type: string
        default: "ci"
      push:
        required: false
        type: boolean
        default: false
    outputs:
      image_path:
        description: Built Docker image path
        value: ${{ jobs.build-panier.outputs.image_path }}

jobs:
  build-panier:
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    outputs:
      image_path: ${{ steps.build.outputs.image_path }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup env
        uses: ./.github/actions/app-env
        with:
          app_name: panier
          json_vars: ${{ toJSON(vars) }}
          json_secrets: ${{ toJSON(secrets) }}
          environment: ${{ inputs.environment }}

      - name: Build panier
        uses: ./.github/actions/app-build
        id: build
        with:
          app_name: panier
          app_hash: ${{ hashFiles('apps/panier', 'packages/api', 'packages/ui', 'apps/backend') }}
          environment: ${{ inputs.environment }}
          password: ${{ secrets.GH_TOKEN }}
          push: ${{ inputs.push }}
