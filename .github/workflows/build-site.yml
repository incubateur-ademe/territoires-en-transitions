name: Build site app

on:
  workflow_call:
    inputs:
      environment:
        required: false
        type: string
        default: "ci"
      push:
        required: false
        type: boolean
        default: false
    outputs:
      image_path:
        description: Built Docker image path
        value: ${{ jobs.build-site.outputs.image_path }}

jobs:
  build-site:
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    outputs:
      image_path: ${{ steps.build.outputs.image_path }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup env
        uses: ./.github/actions/app-env
        with:
          app_name: site
          json_vars: ${{ toJSON(vars) }}
          json_secrets: ${{ toJSON(secrets) }}
          environment: ${{ inputs.environment }}

      - name: Build site
        uses: ./.github/actions/app-build
        id: build
        with:
          app_name: site
          app_hash: ${{ hashFiles('apps/site', 'packages/api', 'packages/ui', 'apps/backend') }}
          environment: ${{ inputs.environment }}
          password: ${{ secrets.GH_TOKEN }}
          push: ${{ inputs.push }}
